apiVersion: v1
kind: Service
metadata:
  name: blockchain101-backend
  namespace: blockchain101
  labels:
    app: blockchain101-backend
spec:
  type: LoadBalancer
  ports:
  - port: 9001
    targetPort: 9001
    protocol: TCP
    name: http
  selector:
    app: blockchain101-backend
